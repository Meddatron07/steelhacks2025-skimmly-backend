services:
  - type: web
    name: steelhacks2025-backend
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn main:app
    # Environment variables will be set manually in Render.com dashboard for security